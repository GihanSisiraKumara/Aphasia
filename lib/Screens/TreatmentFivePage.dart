import 'package:flutter/material.dart';

class TreatmentFivePage extends StatelessWidget {
  const TreatmentFivePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Trueatment Five")),
      body: const Center(
        child: Text("This i s Treatment Five Page"),
      ),
    );
  }
}
