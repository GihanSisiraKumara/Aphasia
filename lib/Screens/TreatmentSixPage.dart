import 'package:flutter/material.dart';

class TreatmentSixPage extends StatelessWidget {
  const TreatmentSixPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("TTreatment Six")),
      body: const Center(
        child: Text("This is Treatment Six Page"),
      ),
    );
  }
}
