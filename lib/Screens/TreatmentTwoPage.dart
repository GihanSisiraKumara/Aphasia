import 'package:flutter/material.dart';

class TreatmentTwoPage extends StatelessWidget {
  const TreatmentTwoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("T reatment Two")),
      body: const Center(
        child: Text("This is Treatment Two Page"),
      ),
    );
  }
}
