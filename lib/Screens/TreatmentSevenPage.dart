import 'package:flutter/material.dart';

class TreatmentSevenPage extends StatelessWidget {
  const TreatmentSevenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("TTreatment Seven")),
      body: const Center(
        child: Text("This is Treatment Seven Page"),
      ),
    );
  }
}
