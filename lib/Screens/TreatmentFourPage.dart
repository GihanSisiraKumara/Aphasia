import 'package:flutter/material.dart';

class TreatmentFourPage extends StatelessWidget {
  const TreatmentFourPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Tre atment Fourk")),
      body: const Center(
        child: Text("This is Treatment Four Page"),
      ),
    );
  }
}
