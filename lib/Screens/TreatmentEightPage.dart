import 'package:flutter/material.dart';

class TreatmentEightPage extends StatelessWidget {
  const TreatmentEightPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("TTreatment Eight")),
      body: const Center(
        child: Text("This is Treatment Eight Page"),
      ),
    );
  }
}
