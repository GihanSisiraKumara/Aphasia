import 'package:flutter/material.dart';

class TreatmentThreePage extends StatelessWidget {
  const TreatmentThreePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("T reatment Three")),
      body: const Center(
        child: Text("This is Treatment Three Pagek"),
      ),
    );
  }
}
